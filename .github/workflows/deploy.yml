name: Deploy React App to S3

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build app
        env:
          REACT_APP_ENV: ${{ secrets.REACT_APP_ENV }}
          REACT_APP_PAYMENTS_URL: ${{ secrets.REACT_APP_PAYMENTS_URL }}
          REACT_APP_SOCKET_URL: ${{ secrets.REACT_APP_SOCKET_URL }}
          REACT_APP_URL: ${{ secrets.REACT_APP_URL }}
          REACT_APP_PROVIDER_URL: ${{ secrets.REACT_APP_PROVIDER_URL }}
          REACT_APP_PREFERENCES_URL: ${{ secrets.REACT_APP_PREFERENCES_URL }}
          REACT_APP_UTILS_URL: ${{ secrets.REACT_APP_UTILS_URL }}
          REACT_APP_UTLIS_URL: ${{ secrets.REACT_APP_UTLIS_URL }}
          REACT_APP_REVIEWS_URL: ${{ secrets.REACT_APP_REVIEWS_URL }}
          REACT_APP_COUPONS_URL: ${{ secrets.REACT_APP_COUPONS_URL }}
          REACT_APP_CHAT_URL: ${{ secrets.REACT_APP_CHAT_URL }}
        run: npm run build:prod

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        with:
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3
        run: aws s3 sync build "s3://${{ secrets.S3_BUCKET }}" --delete

      - name: Invalidate CloudFront
        run: aws cloudfront create-invalidation --distribution-id "${{ secrets.CLOUDFRONT_DISTRIBUTION_ID }}" --paths "/*"
